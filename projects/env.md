# ENV the cli enviornment tool
> Sorry this is empty... i promise i will some stuff here once i get here.. dont you loose hope, knowledge is on its way!

Env is a small cli tool, wich provides a TUI Interface to interact with the Environment varibles(only windows is tested, linux might work on a read-only basis) and add new Ones directly from the terminal/console.

I miss the ability to also add varibles to .env files and read them. All primary interaction with the varibles go through the Evironment namespace in c# and the CLI is just an interface to interact with them.
